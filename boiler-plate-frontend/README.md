# Boilerplate Frontend

Simple React + Vite frontend for the MERN starter.

Run locally
```powershell
cd boiler-plate-frontend
npm install
npm run dev
```

Environment
- VITE_API_URL (defaults to http://localhost:4000)
